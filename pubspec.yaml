name: georescue_360
publish_to: "none"
description: Urban Signs - Sistema movil de gestion comercial para carteleria y gigantografias (Diplomado UAJMS).
version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  go_router: ^17.2.3
  supabase_flutter: ^2.17.2
  shared_preferences: ^2.5.5
  geolocator: ^14.0.3
  flutter_map: ^8.3.1
  latlong2: ^0.10.1
  image_picker: ^1.2.2
  http: ^1.6.0
  intl: ^0.20.3
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
